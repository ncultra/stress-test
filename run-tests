#!/usr/bin/env bash

while [[ 1 ]]
do
    echo "running stress tests"
    ./run-shell-test.sh $1 dd if=/dev/urandom | bzip2 &>/dev/null &
    ./run-shell-test.sh $1 ./eat-cpu.sh

#	./build-kernel.sh 
     wait
done
